<h1>New Favorite</h1>
<%= form_with(model: favorite, local: true) do |form| %>
  <% if favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id_id %>
    <%= form.text_field :user_id_id %>
  </div>

  <div class="field">
    <%= form.label :team_id_id %>
    <%= form.text_field :team_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= render 'form', favorite: @favorite %>

<%= link_to 'Back', favorites_path %>
